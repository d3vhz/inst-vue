import type { VariantProps } from 'class-variance-authority';

import type { alertVariants } from '../config';

type AlertVariants = VariantProps<typeof alertVariants>;

export type { AlertVariants };
